# Data Directory

This folder is reserved for lightweight assets that make the repository easy to run and understand.

- `sample/`: small synthetic records used by the default demo pipeline
- `large_files/`: placeholder path ignored by Git for heavyweight datasets
- `processed/`: optional local location for preprocessed NumPy arrays if you want to run the full research pipeline
